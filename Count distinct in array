class Solution {
    static int countDistinct(int arr[]) {
        HashSet<Integer>hset=new HashSet<>();
        for(int i=0;i<arr.length;i++){
            hset.add(arr[i]);
        }
      return hset.size();  
    }
}
